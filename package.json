{
  "name": "fundacion-peludos",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "astro": "astro",
    "build": "astro build",
    "cf-typegen": "wrangler types",
		"check": "astro build && tsc && wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
    "dev": "astro dev",
		"preview": "astro build && wrangler dev"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12.6.12",
    "@astrojs/sitemap": "3.6.1",
    "astro": "5.16.9"
  },
  "devDependencies": {
    "wrangler": "^4.59.2"
  },
  "engines": {
		"node": ">=22"
	}
}
